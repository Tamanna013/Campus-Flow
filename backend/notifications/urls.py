from django.urls import path
# Import your views here, for example:
# from .views import NotificationList

urlpatterns = [
    # Add at least one path, even if it's just a placeholder
    # path('', NotificationList.as_view(), name='notification-list'),
]